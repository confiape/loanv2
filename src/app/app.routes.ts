import { Routes } from '@angular/router';
import {MainLayoutComponent} from '@loan/app/layout';
import {Select} from '@loan/app/shared/components/select';

export const routes: Routes = [
  {
    path: '',
    component: MainLayoutComponent,

  }
];
